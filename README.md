# polishai
Chrome extension + companion desktop app that rewrites selected text anywhere on the web using AI. Detects text selection across sites, replaces it in place via a Python/AWS Lambda backend, with DynamoDB-backed usage tracking and support for multiple LLM providers (Groq default, or bring your own key).
